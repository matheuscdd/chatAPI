import session from "./session.controller";
import user from "./user.controller";
import talk from "./talk.controller";
import message from "./message.controller";

export default {
    message,
    user,
    session,
    talk
}